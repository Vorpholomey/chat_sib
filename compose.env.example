# Copy to compose.env (gitignored) and adjust. Used by: docker compose --env-file compose.env up --build
#
# Required when DEBUG is false (production): at least 32 random characters.
SECRET_KEY=REWmIbmlOzH4u1WnMhYwptSEWDiUxNHWuuDKMV8zlnw

# Optional overrides (defaults match backend/.env.example-style local creds)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=chat_db

# Published UI port (host): http://localhost:8080
# HTTP_PORT=8080
