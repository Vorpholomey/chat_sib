"""Stable client-visible strings for auth outcomes (keep in sync with frontend `lib/authErrors.ts`)."""

ACCOUNT_PERMANENTLY_BANNED = "Your account has been permanently banned."

# Password recovery / temporary-password flows (stable machine-oriented codes).
TEMPORARY_PASSWORD_EXPIRED = "TEMPORARY_PASSWORD_EXPIRED"
PASSWORD_CHANGE_REQUIRED = "PASSWORD_CHANGE_REQUIRED"
